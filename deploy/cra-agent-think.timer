[Unit]
Description=the thinking agent answers a question every 30 minutes (it spends from its own wallet: enable on purpose)

[Timer]
OnCalendar=*-*-* *:00/30:00
RandomizedDelaySec=60
Persistent=false

[Install]
WantedBy=timers.target

[Unit]
Description=the thinking agent answers a question every three hours (it spends from its own wallet: enable on purpose)

[Timer]
OnCalendar=*-*-* 00/3:20:00
RandomizedDelaySec=300
Persistent=false

[Install]
WantedBy=timers.target

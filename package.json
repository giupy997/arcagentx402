{
  "name": "cra-agent",
  "private": true,
  "version": "0.0.1",
  "description": "Agentic payment rail on Arc. Phase 0: block-zero data collector.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b --force packages/accounting packages/policy packages/ledger packages/identity packages/router packages/collector packages/seller packages/api packages/mcp && npm run web:build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b packages/accounting packages/policy packages/ledger packages/identity packages/router packages/collector packages/seller packages/api packages/mcp && tsc -p packages/web/tsconfig.json --noEmit",
    "lint": "tsc -b --noEmit packages/accounting packages/collector",
    "collector": "npm run start -w @cra-agent/collector --",
    "db:migrate": "npm run migrate -w @cra-agent/collector --",
    "api": "npm run start -w @cra-agent/api --",
    "web:build": "npm run build -w @cra-agent/web",
    "mcp": "npm run start -w @cra-agent/mcp --",
    "rail": "npm run -s cli -w @cra-agent/mcp --"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/pg": "^8.15.0",
    "fast-check": "^4.1.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
